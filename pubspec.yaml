name: jikan
description: Dart wrapper for the Jikan API.
version: 0.0.1
author: Sandro Marques
homepage:

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  http: ^0.12.0
  json_annotation: ^2.0.0

dev_dependencies:
  test: ^1.3.3

  build_verify: ^1.0.0
  build_runner: ^1.0.0
  json_serializable: ^2.0.0
